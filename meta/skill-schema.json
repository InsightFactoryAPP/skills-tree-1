{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/SamoTech/skills-tree/raw/main/meta/skill-schema.json",
  "title": "AI Agent Skill",
  "description": "Schema for a single AI Agent Skill file in the SamoTech/skills-tree repository.",
  "type": "object",
  "required": ["title", "category", "level", "stability", "added", "description"],
  "properties": {
    "title": {
      "type": "string",
      "description": "Human-readable skill name, e.g. 'Chain-of-Thought Reasoning'"
    },
    "category": {
      "type": "string",
      "description": "Category slug, e.g. '02-reasoning'",
      "enum": [
        "01-perception",
        "02-reasoning",
        "03-memory",
        "04-action-execution",
        "05-code",
        "06-communication",
        "07-tool-use",
        "08-multimodal",
        "09-agentic-patterns",
        "10-computer-use",
        "11-web",
        "12-data",
        "13-creative",
        "14-security",
        "15-orchestration",
        "16-domain-specific",
        "17-infrastructure"
      ]
    },
    "level": {
      "type": "string",
      "description": "Skill difficulty level",
      "enum": ["basic", "intermediate", "advanced", "experimental"]
    },
    "stability": {
      "type": "string",
      "description": "How stable/mature this skill is",
      "enum": ["stable", "evolving", "experimental", "deprecated"]
    },
    "version": {
      "type": "string",
      "description": "Skill document version, e.g. 'v1', 'v2', 'v3'",
      "pattern": "^v[0-9]+$"
    },
    "added": {
      "type": "string",
      "description": "Month when the skill was first added, format YYYY-MM",
      "pattern": "^[0-9]{4}-[0-9]{2}$"
    },
    "last_updated": {
      "type": "string",
      "description": "Month of last update, format YYYY-MM",
      "pattern": "^[0-9]{4}-[0-9]{2}$"
    },
    "updated": {
      "type": "string",
      "description": "Canonical month of last update used by current skill files, format YYYY-MM",
      "pattern": "^[0-9]{4}-[0-9]{2}$"
    },
    "description": {
      "type": "string",
      "description": "1-3 sentence description of what this skill enables an agent to do"
    },
    "tags": {
      "type": "array",
      "description": "Short searchable tags associated with the skill",
      "items": { "type": "string" }
    },
    "related": {
      "type": "array",
      "description": "Related skill identifiers or file slugs",
      "items": { "type": "string" }
    },
    "related_skills": {
      "type": "array",
      "description": "List of related skill file paths relative to the skills/ root",
      "items": { "type": "string" }
    },
    "prerequisites": {
      "type": "array",
      "description": "Skill identifiers that should be learned first",
      "items": { "type": "string" }
    },
    "frameworks": {
      "type": "array",
      "description": "Frameworks or models that implement this skill",
      "items": {
        "type": "object",
        "required": ["name", "url"],
        "properties": {
          "name": { "type": "string" },
          "url": { "type": "string", "format": "uri" },
          "implementation": { "type": "string" },
          "since": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
